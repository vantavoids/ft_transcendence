# API Contract - Chat Service

The Chat Service has two interfaces:
1. **REST** - message history, edit/delete
2. **SignalR WebSocket hub** - real-time send/receive, presence

Base path: `/chat` (REST), `/hubs/chat` (SignalR)

All REST responses are `application/json`. Errors follow the shape:
```json
{ "error": "human-readable message" }
```

All endpoints require `Authorization: Bearer <access_token>`.

> **ID policy:** all IDs are Snowflake `BIGINT`s generated by the service. On the JSON wire they appear as quoted strings (e.g. `"123456789012345678"`) so JS clients can hold them without precision loss.

> **Storage note:** Chat Service uses **ScyllaDB** (Cassandra-compatible wide-column store).
> - Channel messages are partitioned by `channel_id`, clustered by `(created_at DESC, id)`.
> - DM messages are partitioned by `conversation_id`, a Snowflake `BIGINT` generated by the Chat Service on the first message between two users and stored in `user_conversations` for both participants so subsequent messages can resolve it without coordination.
> - **Pagination is cursor-based using `before_time` (ISO 8601 timestamp) - not offset.** ScyllaDB has no concept of row offsets; all range scans use clustering key bounds.
> - Edit and delete by message `id` alone require an internal auxiliary lookup (`message_id → channel_id + created_at`). This is transparent to API callers.

---

## REST Endpoints

### GET /channels/{channel_id}/messages

Fetch paginated message history for a channel.

**Query params:**
| Param | Type | Description |
|-------|------|-------------|
| `before_time` | ISO 8601 timestamp | Cursor - return messages with `created_at < before_time`. Use the `created_at` of the oldest message in the previous page. Omit for the first page (returns the most recent messages). |
| `limit` | int | Number of messages to return (default 50, max 100) |

**Response `200`:**
```json
[
  {
    "id": "<snowflake>",
    "channel_id": "<snowflake>",
    "author_id": "<snowflake>",
    "content": "P'tit chien enragé?",
    "reply_to_id": null,
    "edited_at": null,
    "created_at": "2026-03-09T12:00:00Z",
    "attachments": [
      {
        "id": "<snowflake>",
        "url": "https://localhost:1443/api/chat/attachments/<id>/screenshot.png",
        "filename": "screenshot.png",
        "size_bytes": 184320,
        "mime_type": "image/png"
      }
    ],
    "reactions": [
      { "emoji": "👍", "count": 3, "me_reacted": true }
    ]
  }
]
```

`reply_to_id` is the `id` of the message this one replies to, or `null` for a top-level message. `attachments` and `reactions` are always present; they are `[]` when there are none. `me_reacted` is `true` iff the caller is among the users who reacted with that emoji.

Messages are returned newest-first. To load more (scroll up), pass `before_time` = the `created_at` of the last (oldest) message in the current page.

Deleted messages (`is_deleted = true` in the schema) are filtered out of the response entirely. Clients learn about deletions in-session via the `MessageDeleted` SignalR event; once the page is reloaded, the deleted message simply does not appear.

**Errors:**
| Status | Reason |
|--------|--------|
| 403 | User is not a member / lacks `READ_MESSAGES` permission |
| 404 | Channel not found |

---

### PATCH /messages/{id}

Edit a message. Only the author can edit their own messages.

**Request body:**
```json
{
  "content": "Updated content"
}
```

**Response `200`:**
```json
{
  "id": "<snowflake>",
  "content": "P'tit coca rigolo?",
  "edited_at": "2026-03-09T12:05:00Z"
}
```

**Errors:**
| Status | Reason |
|--------|--------|
| 403 | Not the author |
| 404 | Message not found |

**Side effects:** Broadcasts `MessageEdited` event to all clients in the SignalR channel group.

---

### DELETE /messages/{id}

Delete a message. Author or member with `MANAGE_MESSAGES` permission.

**Response `204`:** No content.

**Errors:**
| Status | Reason |
|--------|--------|
| 403 | Not the author and lacks `MANAGE_MESSAGES` |
| 404 | Message not found |

**Side effects:** Broadcasts `MessageDeleted { message_id, channel_id }` to the SignalR channel group.

---

### GET /dms

List all DM conversations for the authenticated user.

**Response `200`:**
```json
[
  {
    "partner_id": "<snowflake>",
    "last_preview": "mec ta partie elle leak de fou",
    "last_message_at": "2026-03-09T11:00:00Z",
    "unread_count": 3
  }
]
```

`last_preview` is the first ~100 characters of the most recent message in the conversation (sidebar preview); not the full message body. Mirrors `user_conversations.last_preview` in the schema.

---

### GET /dms/{user_id}/messages

Fetch DM message history with a specific user.

**Query params:**
| Param | Type | Description |
|-------|------|-------------|
| `before_time` | ISO 8601 timestamp | Cursor - return messages with `created_at < before_time`. Omit for the first page. |
| `limit` | int | Default 50, max 100 |

**Response `200`:**
```json
[
  {
    "id": "<snowflake>",
    "sender_id": "<snowflake>",
    "recipient_id": "<snowflake>",
    "content": "faut normer ta partie d'ailleurs :wittle_guy:",
    "reply_to_id": null,
    "created_at": "2026-03-09T11:00:00Z",
    "attachments": []
  }
]
```

`reply_to_id` is the `id` of the DM this one replies to, or `null` for a top-level message. `attachments` follows the same shape as the channel-message response.

Deleted DMs are filtered out (same rule as channel messages).

**Errors:**
| Status | Reason |
|--------|--------|
| 404 | No DM conversation found |

---

## Attachment Endpoints

Two-step upload flow:
1. `POST /attachments` to upload the file - returns a draft attachment record with an `id`.
2. Reference one or more attachment IDs in `SendMessage` / `SendDirectMessage`. The server attaches them to the message at persist time.

Draft attachments are owned by the uploader and live for 1 hour. After that they are eligible for cleanup. Once attached to a message they are permanent (subject to message deletion).

Limits:
- Max file size: **25 MB** per file.
- Max attachments per message: **10**.
- Blocked MIME types: executables and scripts (`application/x-msdownload`, `application/x-executable`, `application/x-mach-binary`, `application/vnd.android.package-archive`, `application/x-sh`).

### POST /attachments

Upload a file. Returns the metadata the client needs to reference the attachment in a `SendMessage` / `SendDirectMessage` call.

**Request:** `Content-Type: multipart/form-data`
- Field `file`: the file blob (binary).

**Response `201`:**
```json
{
  "id": "<snowflake>",
  "url": "https://localhost:1443/api/chat/attachments/<id>/screenshot.png",
  "filename": "screenshot.png",
  "size_bytes": 184320,
  "mime_type": "image/png"
}
```

The returned `url` is what clients render in the message UI; it routes through `GET /attachments/{id}/{filename}` and is auth-checked on every request.

**Errors:**
| Status | Reason |
|--------|--------|
| 400 | Missing file field, file empty, or size > 25 MB |
| 415 | Blocked MIME type |

---

### GET /attachments/{id}/{filename}

Download an attachment's underlying blob. Streamed by Chat Service from object storage; never served directly from the storage layer so authorization stays in one place.

Authorization:
- For a channel attachment: caller must be a member of the channel's guild with `READ_MESSAGES` on the channel.
- For a DM attachment: caller must be one of the two participants of the conversation.
- For a draft attachment (not yet attached to a message): only the uploader can fetch it.

**Response `200`:** Binary stream with `Content-Type` = stored `mime_type`, `Content-Disposition: inline; filename="<filename>"`.

**Errors:**
| Status | Reason |
|--------|--------|
| 403 | Not authorized to view this attachment |
| 404 | Attachment not found, or filename mismatch |

---

## Read State Endpoints

Read state is **per-user, per-channel**: the snowflake of the last message the user has read, plus the timestamp it was set. The Chat Service uses these to compute unread counts and to drive the "new messages" divider in the UI. DMs use their own unread counter (`dm_unread_counts`) and are surfaced through `GET /dms`.

### PUT /channels/{id}/read

Advance the caller's read cursor for a channel to a specific message. Idempotent and monotonic - calling with a `message_id` older than the current cursor is a no-op.

**Request body:**
```json
{
  "message_id": "<snowflake>"
}
```

**Response `200`:**
```json
{
  "channel_id": "<snowflake>",
  "last_read_message_id": "<snowflake>",
  "unread_count": 0
}
```

**Errors:**
| Status | Reason |
|--------|--------|
| 403 | Caller cannot read this channel |
| 404 | Channel or message not found |

**Side effects:** Sends `ReadStateUpdated` to the caller's personal SignalR group so other devices sync.

---

### GET /channels/read-states

Fetch the caller's read state across every channel they can see. Intended for sidebar unread badges - cheaper than calling `/channels/{id}/read` for each channel.

**Response `200`:**
```json
[
  {
    "channel_id": "<snowflake>",
    "last_read_message_id": "<snowflake>",
    "unread_count": 3
  }
]
```

Channels with no read-state row yet (never read by this user) are returned with `last_read_message_id: null` and `unread_count` = total non-deleted messages in the channel.

---

## Reaction Endpoints

Reactions are supported on **channel messages only** (the schema does not model DM reactions). The emoji is a URL-encoded UTF-8 string and acts as the natural key; one user can react to a given message with a given emoji at most once.

### PUT /messages/{id}/reactions/{emoji}

Add the caller's reaction to a channel message. Idempotent: repeating the call is a no-op. Requires `READ_MESSAGES` on the channel that owns the message.

**Response `200`:**
```json
{
  "emoji": "👍",
  "count": 4,
  "me_reacted": true
}
```

**Errors:**
| Status | Reason |
|--------|--------|
| 403 | Caller cannot read this channel |
| 404 | Message not found, or message is a DM |

**Side effects:** Broadcasts `ReactionAdded` to the SignalR channel group.

---

### DELETE /messages/{id}/reactions/{emoji}

Remove the caller's own reaction. Removing a reaction the caller did not place is a no-op (returns the current count).

**Response `200`:**
```json
{
  "emoji": "👍",
  "count": 3,
  "me_reacted": false
}
```

**Errors:**
| Status | Reason |
|--------|--------|
| 403 | Caller cannot read this channel |
| 404 | Message not found, or message is a DM |

**Side effects:** Broadcasts `ReactionRemoved` to the SignalR channel group when the caller actually had a reaction to remove.

---

## SignalR Hub - `/hubs/chat`

Connect with the access token:
```
wss://<host>/hubs/chat?access_token=<jwt>
```

On connect, the server:
1. Adds the client to all SignalR groups for their guild channels, plus a personal group for DMs.
2. Checks for a pending incoming call (caller sent `CallOffer` while this user was offline) - if one exists, immediately relays `IncomingCall` to the client so they can answer.

---

### Client → Server (Invocations)

#### SendMessage

Send a message to a channel.

```json
{
  "target": "SendMessage",
  "arguments": [{
    "channel_id": "<snowflake>",
    "content": "toi multithreading",
    "reply_to_id": null,
    "attachment_ids": []
  }]
}
```

`reply_to_id` is optional - set it to the `id` of an existing message in the same channel to thread a reply. Omit or pass `null` for a top-level message. The server validates that the referenced message exists in the same channel and is not deleted.

`attachment_ids` is optional - an array of draft attachment IDs returned by `POST /attachments` (max 10). The server validates that each attachment was uploaded by the caller, has not yet been attached to another message, and has not expired.

Server actions:
1. HTTP GET to Guild Service `/internal/channels/{channel_id}/membership?user_id=...` over the docker network - validates membership + `SEND_MESSAGES` permission
2. Persists message in DB
3. Broadcasts `ReceiveMessage` to the channel group
4. Publishes `chat.message_sent` to RabbitMQ

**Error:** Hub sends `Error` event back to the caller if validation fails.

---

#### SendDirectMessage

Send a DM to another user.

```json
{
  "target": "SendDirectMessage",
  "arguments": [{
    "recipient_id": "<snowflake>",
    "content": "devine qui c'est qui tourne :)",
    "reply_to_id": null,
    "attachment_ids": []
  }]
}
```

`reply_to_id` is optional - set it to the `id` of an existing DM in the same conversation to thread a reply. The server validates that the referenced message belongs to the same conversation and is not deleted.

`attachment_ids` follows the same rules as `SendMessage` (max 10, ownership checked).

Server actions:
1. Persists the message in DB (creates the conversation if it does not exist yet).
2. Broadcasts `ReceiveDirectMessage` to the recipient's personal group.
3. Publishes `chat.dm_sent` to RabbitMQ so Notification Service can create a `dm` notification.

---

### Server → Client (Events)

#### ReceiveMessage

Broadcast to all members of a channel when a new message arrives.

```json
{
  "target": "ReceiveMessage",
  "arguments": [{
    "id": "<snowflake>",
    "channel_id": "<snowflake>",
    "author_id": "<snowflake>",
    "content": "viens on fait minishell",
    "reply_to_id": null,
    "created_at": "2026-03-09T12:00:00Z",
    "attachments": [],
    "reactions": []
  }]
}
```

`attachments` and `reactions` carry the same shape as in the REST `GET /channels/{channel_id}/messages` response. `reactions` is always `[]` on a newly-sent message (counts grow via `ReactionAdded`).

---

#### ReceiveDirectMessage

Sent to the recipient's personal group when a DM arrives.

```json
{
  "target": "ReceiveDirectMessage",
  "arguments": [{
    "id": "<snowflake>",
    "sender_id": "<snowflake>",
    "content": "y a des merge conflicts d'ailleurs",
    "reply_to_id": null,
    "created_at": "2026-03-09T12:00:00Z",
    "attachments": []
  }]
}
```

---

#### MessageEdited

```json
{
  "target": "MessageEdited",
  "arguments": [{
    "id": "<snowflake>",
    "channel_id": "<snowflake>",
    "content": "et la p'tite update du message là mhmmm",
    "edited_at": "2026-03-09T12:05:00Z"
  }]
}
```

---

#### MessageDeleted

```json
{
  "target": "MessageDeleted",
  "arguments": [{
    "message_id": "<snowflake>",
    "channel_id": "<snowflake>"
  }]
}
```

---

#### ReactionAdded

Broadcast to the channel group when a user reacts to a channel message. Carries the post-mutation count so clients can refresh the displayed total without recomputing.

```json
{
  "target": "ReactionAdded",
  "arguments": [{
    "message_id": "<snowflake>",
    "channel_id": "<snowflake>",
    "user_id": "<snowflake>",
    "emoji": "👍",
    "count": 4
  }]
}
```

---

#### ReactionRemoved

Broadcast to the channel group when a user removes their reaction. Same shape as `ReactionAdded`.

```json
{
  "target": "ReactionRemoved",
  "arguments": [{
    "message_id": "<snowflake>",
    "channel_id": "<snowflake>",
    "user_id": "<snowflake>",
    "emoji": "👍",
    "count": 3
  }]
}
```

---

#### ReadStateUpdated

Sent to the caller's personal SignalR group after `PUT /channels/{id}/read` so other devices the user is connected on update their unread badges.

```json
{
  "target": "ReadStateUpdated",
  "arguments": [{
    "channel_id": "<snowflake>",
    "last_read_message_id": "<snowflake>",
    "unread_count": 0
  }]
}
```

---

#### UserPresence

Broadcast to all shared guild members when a user connects or disconnects.

```json
{
  "target": "UserPresence",
  "arguments": [{
    "user_id": "<snowflake>",
    "status": "online"
  }]
}
```

`status` is `online` or `offline`.

---

## WebRTC Signaling (via SignalR Hub)

Voice and video calls are **1-on-1, peer-to-peer via WebRTC**. The Chat Service hub acts as the signaling server - it relays SDP offers/answers and ICE candidates between the two peers. No media ever touches the server.

### Call flow

```
Caller                      Chat Hub                     Callee
  │                             │                            │
  ├─ CallOffer ────────────────►│                            │
  │  { callee_id, call_id,      │─── IncomingCall ──────────►│
  │    sdp, call_type }         │    { caller_id, call_id,   │
  │                             │      sdp, call_type }      │
  │                             │                            │
  │                             │◄── CallAnswer ─────────────┤
  │◄─── CallAnswered ───────────┤    { call_id, sdp }        │
  │     { call_id, sdp }        │                            │
  │                             │                            │
  ├─ IceCandidate ─────────────►│─── IceCandidate ──────────►│
  │◄─── IceCandidate ───────────┤◄── IceCandidate ───────────┤
  │     (relay, both ways)      │    (relay, both ways)      │
  │                             │                            │
  │  [P2P connection established - media flows directly]     │
  │                             │                            │
  ├─ CallHangup ───────────────►│─── CallHungUp ────────────►│
     { call_id }                │    { call_id }
```

If the callee **rejects** the call:
```
Callee ── CallReject ──► Hub ── CallRejected ──► Caller
```

If the callee is **busy**, the Hub immediately replies to the caller with `CallFailed { reason: "user_busy" }`.

If the callee is **offline**, the Hub publishes `call.incoming` to RabbitMQ (Notification Service notifies them) and keeps the call pending. `IncomingCall` is relayed when the callee next connects to the hub.

If the callee does not answer within **2 minutes**, the Hub sends `CallFailed { reason: "timeout" }` to the caller and clears call state.

---

### Client → Server (Invocations) - Signaling

#### CallOffer

Initiate a call. Caller generates `call_id` (Snowflake).

```json
{
  "target": "CallOffer",
  "arguments": [{
    "callee_id": "<snowflake>",
    "call_id": "<snowflake>",
    "call_type": "video",
    "sdp": "<SDP offer string>"
  }]
}
```

`call_type`: `"audio"` or `"video"`.

Server actions:
1. Check callee is not already in a call → if busy, send `CallFailed { call_id, reason: "user_busy" }` to caller
2. Check callee is connected to hub
   - **Connected** → relay `IncomingCall` to callee's personal group immediately
   - **Not connected** → publish `call.incoming` to RabbitMQ `{ call_id, caller_id, callee_id, call_type }`. Notification Service will notify the callee. `IncomingCall` will be relayed when the callee next connects to the hub (see OnConnect above).
3. Store the pending call state server-side (caller, callee, sdp, call_type)
4. Start a 2-minute answer timeout - on expiry, send `CallFailed { call_id, reason: "timeout" }` to caller and clear call state

---

#### CallAnswer

Accept an incoming call. Callee responds with their SDP answer.

```json
{
  "target": "CallAnswer",
  "arguments": [{
    "call_id": "<snowflake>",
    "sdp": "<SDP answer string>"
  }]
}
```

Server relays `CallAnswered` to the caller's personal group.

---

#### CallReject

Decline an incoming call.

```json
{
  "target": "CallReject",
  "arguments": [{
    "call_id": "<snowflake>"
  }]
}
```

Server relays `CallRejected` to the caller's personal group.

---

#### CallHangup

End an ongoing or ringing call (usable by either party at any point after `CallOffer`).

```json
{
  "target": "CallHangup",
  "arguments": [{
    "call_id": "<snowflake>"
  }]
}
```

Server relays `CallHungUp` to the other party's personal group and clears call state.

---

#### IceCandidate

Relay a trickle ICE candidate to the remote peer.

```json
{
  "target": "IceCandidate",
  "arguments": [{
    "call_id": "<snowflake>",
    "candidate": "<ICE candidate string>",
    "sdp_mid": "<string>",
    "sdp_mline_index": 0
  }]
}
```

Server relays `IceCandidate` verbatim to the other party's personal group.

---

### Server → Client (Events) - Signaling

#### IncomingCall

Sent to the callee's personal group when someone calls them.

```json
{
  "target": "IncomingCall",
  "arguments": [{
    "call_id": "<snowflake>",
    "caller_id": "<snowflake>",
    "call_type": "video",
    "sdp": "<SDP offer string>"
  }]
}
```

---

#### CallAnswered

Sent to the caller when the callee accepts.

```json
{
  "target": "CallAnswered",
  "arguments": [{
    "call_id": "<snowflake>",
    "sdp": "<SDP answer string>"
  }]
}
```

---

#### CallRejected

Sent to the caller when the callee declines.

```json
{
  "target": "CallRejected",
  "arguments": [{
    "call_id": "<snowflake>"
  }]
}
```

---

#### CallHungUp

Sent to the other party when someone ends the call.

```json
{
  "target": "CallHungUp",
  "arguments": [{
    "call_id": "<snowflake>"
  }]
}
```

---

#### CallFailed

Sent to the caller when the call cannot be established (callee busy or timeout).

```json
{
  "target": "CallFailed",
  "arguments": [{
    "call_id": "<snowflake>",
    "reason": "user_busy"
  }]
}
```

`reason`: `"user_busy"` | `"timeout"`

---

#### IceCandidate

Relayed verbatim from the remote peer.

```json
{
  "target": "IceCandidate",
  "arguments": [{
    "call_id": "<snowflake>",
    "candidate": "<ICE candidate string>",
    "sdp_mid": "<string>",
    "sdp_mline_index": 0
  }]
}
```

---

## RabbitMQ Events Published

| Event | Payload | Trigger |
|-------|---------|---------|
| `chat.message_sent` | `{ channel_id, guild_id, author_id, message_id, content, mentions: [snowflake] }` | Message sent in a channel |
| `chat.dm_sent` | `{ conversation_id, message_id, sender_id, recipient_id, content }` | DM sent to another user |
| `call.incoming` | `{ call_id, caller_id, callee_id, call_type }` | Callee was offline when `CallOffer` arrived - Notification Service notifies them to open the app |
| `user.online` | `{ user_id }` | Client connects to hub |
| `user.offline` | `{ user_id }` | Client disconnects from hub |

## RabbitMQ Events Consumed

| Event | Action |
|-------|--------|
| `guild.member_joined` | Add user to SignalR groups for all channels in that guild |
| `guild.member_left` | Remove user from SignalR groups for that guild |
| `user.logged_out` | Force-disconnect any active WebSocket for that user |
| `user.deleted` | Force-disconnect any active WebSocket; delete the user's `user_conversations`, `channel_read_states`, and `dm_unread_counts` rows. Messages stay - the user's profile becomes unresolvable in User Service, so the frontend renders them as "Deleted User". |
