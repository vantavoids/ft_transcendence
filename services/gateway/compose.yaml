services:
  gateway-service:
    container_name: gateway-container
    image: gateway-image
    pull_policy: build
    build: .
    ports:
      - "8090:8090"
    env_file: .env
    restart: unless-stopped
