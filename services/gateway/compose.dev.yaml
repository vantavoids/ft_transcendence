services:
  gateway:
    restart: unless-stopped
    container_name: gateway
    image: gateway-service
    pull_policy: build
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./:/app
