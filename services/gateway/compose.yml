services:
  gateway-service:
    restart: unless-stopped
    container_name: gateway-container
    image: gateway-image
    pull_policy: build
    build: 
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "8090:8090"
