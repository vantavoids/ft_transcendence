services:
  auth-service:
    container_name: auth-service
    build:
      context: .
      dockerfile: Dockerfile
    image: auth-service:local
    ports:
      - "8080:8080"
    env_file: .env
    restart: unless-stopped
